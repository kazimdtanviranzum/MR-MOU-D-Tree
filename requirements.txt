# Python dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
scipy>=1.10
python-docx>=1.1
pillow>=10.0

# External (not pip-installable) requirement:
#   pandoc  - required by src/build_manuscript.py to convert the LaTeX in
#             equations/equations.tex into native, editable Office Math (OMML)
#             equation objects in the .docx.  Install from https://pandoc.org
#             or via your package manager (e.g. apt install pandoc).
